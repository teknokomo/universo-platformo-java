# [Название пакета] [Фронтенд/Бэкенд]

✨ **Современный пакет** - Часть архитектуры Universo Platformo Java

## Обзор

[Краткое описание назначения и функциональности пакета. Опишите трёхуровневую архитектуру, если применимо: Первичные → Вторичные → Третичные сущности]

## Информация о пакете

- **Пакет**: `pro.universo.[feature]-[frt/srv]`
- **Версия**: `0.1.0`
- **Тип**: Пакет [Vaadin фронтенд / Spring бэкенд]
- **Фреймворк**: [Vaadin 24.x / Spring Boot 3.x]
- **Система сборки**: Maven
- **Тестирование**: JUnit 5, [Vaadin TestBench / Spring Test]

## Ключевые возможности

### 🌍 Управление [функцией]
- **Иерархическая организация**: Трёхуровневая архитектура ([Первичные] → [Вторичные] → [Третичные])
- **Полная изоляция данных**: [Сущности] из разных [первичных сущностей] полностью разделены
- **Управление доступом на основе ролей**: Роли пользователей и разрешения для управления [функцией]
- **Контекстно-зависимая навигация**: Навигация с учётом [функции] с хлебными крошками

### 🎨 Пользовательский интерфейс (только для фронтенд-пакетов)
- **Интеграция Vaadin**: Согласованные UI-компоненты с темой Lumo
- **Адаптивный дизайн**: Оптимизирован для настольных и мобильных устройств
- **Представления сетки**: Гибкое представление данных с пагинацией и поиском
- **Диалоговые формы**: Модальные формы для создания и редактирования сущностей

### 🔧 Технические возможности
- **Типобезопасность**: Полная типобезопасность Java с дженериками
- **Доступ к данным**: JPA-репозитории с Spring Data
- **Интернационализация**: Переводы на английский и русский с ResourceBundle
- **Валидация форм**: Bean Validation (JSR-380) с пользовательскими валидаторами
- **Интеграция API**: RESTful API с Spring Web

## Установка и настройка

### Предварительные требования
```bash
# Системные требования
Java 17 или новее (LTS)
Maven 3.9.x или новее
База данных PostgreSQL (или аккаунт Supabase)
```

### Установка
```bash
# Установить зависимости
mvn clean install

# Собрать пакет
mvn --projects packages/[feature]-[frt/srv]/base clean package

# Запустить в режиме разработки
mvn --projects packages/[feature]-[frt/srv]/base spring-boot:run
```

### Интеграция
```java
// Импортировать в ваше Spring Boot приложение (Бэкенд)
import pro.universo.[feature].api.controller.[Feature]Controller;
import pro.universo.[feature].api.service.[Feature]Service;

// Импортировать в ваше Vaadin приложение (Фронтенд)
import pro.universo.[feature].ui.views.[Feature]ListView;
```

## Архитектура

### Трёхуровневая модель сущностей
- **[Первичные]**: Организационные единицы верхнего уровня, обеспечивающие полную изоляцию данных
- **[Вторичные]**: Логические группировки внутри [первичных] (например, "Категории", "Группы")
- **[Третичные]**: Отдельные элементы, принадлежащие конкретным [вторичным] внутри [первичных]

### Стратегия изоляции данных
- Полное разделение между [первичными сущностями] - нет межуровневой видимости
- Все операции поддерживают контекст [первичных] через URL-маршрутизацию
- Валидация на фронтенде и бэкенде, предотвращающая осиротевшие сущности
- Управление доступом на основе ролей для разрешений [первичных]

## Использование

### Базовые компоненты (Фронтенд)
```java
@Route(value = "[feature]", layout = MainLayout.class)
@PageTitle("[Feature]")
@PermitAll
public class [Feature]ListView extends VerticalLayout {
    
    private final [Feature]Service service;
    private final Grid<[Feature]> grid = new Grid<>([Feature].class);
    
    public [Feature]ListView([Feature]Service service) {
        this.service = service;
        configureGrid();
        add(createToolbar(), grid);
    }
}
```

### Интеграция API (Бэкенд)
```java
@RestController
@RequestMapping("/api/[feature]")
public class [Feature]Controller {
    
    private final [Feature]Service service;
    
    @GetMapping
    public ResponseEntity<List<[Feature]Response>> list() {
        return ResponseEntity.ok(service.findAll());
    }
    
    @PostMapping
    public ResponseEntity<[Feature]Response> create(
            @Valid @RequestBody Create[Feature]Request request) {
        return ResponseEntity.status(HttpStatus.CREATED)
            .body(service.create(request));
    }
}
```

## Структура файлов

```
packages/[feature]-[frt/srv]/base/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── pro/universo/[feature]/[api/ui]/
│   │   │       ├── controller/    # REST-эндпоинты (бэкенд)
│   │   │       ├── service/       # Бизнес-логика
│   │   │       ├── repository/    # Доступ к данным (бэкенд)
│   │   │       ├── model/         # JPA-сущности (бэкенд)
│   │   │       ├── dto/           # Объекты передачи данных
│   │   │       ├── views/         # Представления Vaadin (фронтенд)
│   │   │       └── components/    # Переиспользуемые UI-компоненты (фронтенд)
│   │   └── resources/
│   │       ├── messages_en.properties  # Интернационализация (английский)
│   │       ├── messages_ru.properties  # Интернационализация (русский)
│   │       ├── application.yml         # Конфигурация
│   │       └── db/migration/           # Миграции базы данных
│   └── test/
│       └── java/
│           └── pro/universo/[feature]/[api/ui]/
│               ├── controller/    # Тесты контроллеров
│               ├── service/       # Модульные тесты сервисов
│               └── repository/    # Тесты репозиториев
├── pom.xml
├── README.md             # Версия на английском
└── README-RU.md          # Этот файл
```

## Разработка

### Предварительные требования
- Java 17+
- Maven 3.9+
- PostgreSQL 15+

### Доступные скрипты
```bash
# Разработка
mvn compile              # Компиляция исходников Java
mvn spring-boot:run      # Запуск с горячей перезагрузкой

# Тестирование
mvn test                 # Запуск набора тестов
mvn test -Dtest=[TestClass]  # Запуск конкретного теста
mvn verify               # Запуск тестов + интеграционные тесты

# Качество кода
mvn checkstyle:check     # Проверка стиля кода
mvn spotbugs:check       # Поиск ошибок
mvn jacoco:report        # Генерация отчёта о покрытии
```

### Рекомендации по разработке

#### Архитектурные паттерны
- **Трёхуровневая модель**: [Первичные] → [Вторичные] → [Третичные]
- **Изоляция данных**: Строгие границы контекста между [первичными сущностями]
- **Spring Data JPA**: Централизованный доступ к данным и транзакции
- **Компоненты Vaadin**: Использование согласованной библиотеки компонентов

#### Валидация
```java
@Entity
public class [Feature] {
    @NotBlank(message = "{[feature].name.required}")
    @Size(max = 255, message = "{[feature].name.maxlength}")
    private String name;
}
```

## Тестирование

### Структура тестов
```
src/test/java/
├── controller/
│   └── [Feature]ControllerTest.java
├── service/
│   └── [Feature]ServiceTest.java
└── repository/
    └── [Feature]RepositoryTest.java
```

### Подход к тестированию
```java
@SpringBootTest
class [Feature]ServiceTest {
    
    @Autowired
    private [Feature]Service service;
    
    @Test
    void shouldCreate[Feature]() {
        Create[Feature]Request request = new Create[Feature]Request();
        request.setName("Test");
        
        [Feature]Response response = service.create(request);
        
        assertNotNull(response.getId());
        assertEquals("Test", response.getName());
    }
}
```

### Запуск тестов
```bash
mvn test                    # Запуск всех тестов
mvn test -Dtest=[Test]*     # Запуск тестов по шаблону
mvn verify                  # Запуск интеграционных тестов
mvn jacoco:report           # Генерация отчёта о покрытии
```

## Конфигурация

### Переменные окружения
```bash
# Конфигурация базы данных
DATABASE_URL=jdbc:postgresql://localhost:5432/universo
DATABASE_USERNAME=your-username
DATABASE_PASSWORD=your-password

# Аутентификация
JWT_SECRET=your-jwt-secret

# Конфигурация приложения
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080
```

## Участие в разработке

### Стиль кода
- Следовать Google Java Style Guide
- Использовать осмысленные имена переменных
- Добавлять Javadoc для публичных методов
- Держать методы небольшими и сфокусированными

### Процесс Pull Request
1. Создать ветку функциональности из `main`
2. Реализовать изменения с тестами
3. Обновить документацию (на английском и русском)
4. Запустить полный набор тестов
5. Отправить PR с описанием

### Соглашение о коммитах
Следовать соглашению о коммитах:
```bash
feat([feature]): добавлена функция поиска
fix([feature]): обработка null-значений
docs([feature]): обновлено руководство по установке
test([feature]): добавлены интеграционные тесты
```

## Связанные пакеты
- [`pro.universo.[feature]-srv`](../[feature]-srv/base/README.md) - Бэкенд-сервис
- [`pro.universo.[feature]-frt`](../[feature]-frt/base/README.md) - Фронтенд UI
- [`pro.universo.core-srv`](../core-srv/base/README.md) - Основные бэкенд-сервисы
- [`pro.universo.core-frt`](../core-frt/base/README.md) - Основной фронтенд UI

---
*Часть [Universo Platformo Java](../../../README.md) - Комплексная реализация платформы*
